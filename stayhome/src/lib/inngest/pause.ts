/**
 * Kill switch for **scheduled** crawling.
 *
 * 현재 **돌고 있다** (2026-08-24 재개, 매일 09:00 KST 1회).
 *
 * 경위: 2026-08-11부터 한화 크롤러를 기다리며 멈춰 있다가, 08-13에 다섯 번째 리조트가
 * 붙어 5곳을 한 번에 켰고 같은 날 다시 껐다. 그 뒤 11일 동안 정기 수집이 없어
 * `resort_inventory`가 통째로 낡았다 — 08-24 시점에 "예약 가능"으로 표시되던 행의
 * 약 3분의 2가 일주일 이상 된 것이었고, 운영자가 실제로 없는 방을 보고 갔다.
 * 그래서 재개했다. 주기는 종전 3시간이 아니라 **하루 1회**다(운영자 결정).
 *
 * ## 다시 켜는 법 / 멈추는 법
 *
 * 이 상수 하나만 바꾸고 배포한다. 그게 전부다 — DB도 Inngest 대시보드도
 * 건드릴 것이 없다. `Resort.active`는 다섯 곳 모두 true다.
 *
 * **멈출 때는 아래 `SCHEDULED_CRAWL_PAUSE_REASON`도 같이 고칠 것.** 낡은 이유가 붙은
 * 일시정지는 "왜 멈춰 있는지 아무도 모르는 상태"와 같다.
 *
 * ## 멈출 때 같이 생각할 것
 *
 * 정기 수집을 끄면 화면의 데이터가 그날부터 늙기 시작하고, 그 늙음은
 * `src/lib/freshness.ts`의 임계값에 따라 조회 화면에서 "확인 필요"로 드러난다.
 * 즉 이 스위치를 끄는 것은 조용한 결정이 아니다 — 며칠 뒤 화면이 회색으로 변한다.
 * 그게 의도된 동작이다. 08-13의 일시정지가 아무 흔적도 남기지 않아서 낡음이
 * 11일 동안 보이지 않았다.
 *
 * ## 왜 크론 트리거를 지우지 않는가
 *
 * `scheduled-refresh`의 cron 표현식은 그대로 두고 함수 **본문**에서 멈춘다.
 * 트리거를 지우면 Inngest 대시보드에 실행이 아예 안 뜨는데, 그 화면은
 * "일부러 껐다"와 "sync가 깨져서 스케줄러가 우리 함수를 모른다"를 구별해주지
 * 않는다. 이 프로젝트는 정확히 후자로 오래 고생했다 — `/api/inngest`가 모듈
 * 로드 단계에서 500이라 크론이 **한 번도** 실행된 적이 없었고, 증상은 "크론이
 * 안 돈다"였지만 원인은 크론 설정이 아니었다 (CLAUDE.md "배포" 절).
 *
 * 하루 한 번 no-op 실행이 남는 편이 낫다. 그것이 스케줄러가 살아 있다는
 * 증거이자, 멈춰 있는 이유를 자기 입으로 말하는 로그다.
 *
 * ## 무엇이 멈추지 않는가
 *
 * 정기 팬아웃 두 경로(`scheduled-refresh`, `/api/cron/refresh` 백스톱)만 멈춘다.
 * `resort/crawl.requested` 이벤트를 직접 발행하는 것과 조회 화면의 "최신화"
 * 버튼(`POST /api/resorts/[slug]/refresh`)은 그대로 동작한다 — 일시정지의 목적은
 * 자동 수집을 미루는 것이지 리조트를 손으로 확인하는 길까지 막는 것이 아니다.
 */
export const SCHEDULED_CRAWL_PAUSED = false;

/**
 * 멈춰 있을 때 로그와 응답에 그대로 실린다. 언제·왜 멈췄는지가 그 자리에서 보이도록.
 * **`SCHEDULED_CRAWL_PAUSED`를 true로 바꾸는 사람이 이 문장도 같이 고친다.**
 * 지금은 돌고 있으므로 이 문자열은 아무 데도 나타나지 않는다.
 */
export const SCHEDULED_CRAWL_PAUSE_REASON =
  "일시정지 중 (사유 미기재 — 멈춘 사람이 이 문장을 고치지 않았다)";
